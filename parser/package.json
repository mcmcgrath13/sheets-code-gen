{
  "name": "parser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "jison sheets_parser.jison && prettier -w sheets_parser.js && cp sheets_parser.js ../src/sheets-parser.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jison": "^0.4.18",
    "prettier": "^2.2.1"
  }
}
